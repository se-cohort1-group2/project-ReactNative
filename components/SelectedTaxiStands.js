import { Text, View, Alert } from "react-native";
import { Marker, Callout } from "react-native-maps";

export default function SelectedTaxiStands({userlocation, selectedLocations}){
    return(
        if (selectedLocations.size > 0){
            
        }
    )
        
}